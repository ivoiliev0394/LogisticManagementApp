@model LogisticManagementApp.Models.CompanyPortal.Shipments.Ulds.CompanyShipmentUldsViewModel

@{
    ViewData["Title"] = "Shipment ULDs";
}

<partial name="_CompanyModuleHeader" />

<div class="row g-4">
    <div class="col-12 col-xl-3">
        <partial name="_CompanyNavigation" />
    </div>

    <div class="col-12 col-xl-9">
        <div class="card">
            <div class="card-body p-4">
                <div class="d-flex justify-content-between align-items-start flex-wrap gap-3 mb-4">
                    <div>
                        <h2 class="h4 mb-1">Shipment ULDs</h2>
                        <p class="text-muted mb-0">@Model.ShipmentNo</p>
                    </div>

                    <div class="d-flex flex-wrap gap-2">
                        @if (Model.CanManageUlds)
                        {
                            <a asp-action="CreateShipmentUld"
                                   asp-route-shipmentId="@Model.ShipmentId"
                                   class="btn btn-primary">
                                Добави ULD
                            </a>
                        }

                        <a asp-action="ShipmentDetails"
                               asp-route-id="@Model.ShipmentId"
                               class="btn btn-outline-light">
                            Назад към shipment
                        </a>
                    </div>
                </div>

                @if (Model.Ulds.Any())
                {
                    <div class="table-responsive" data-table-pagination-wrapper>
                        <table class="table align-middle" data-table-pagination="true" data-page-size="8">
                            <thead>
                                <tr>
                                    <th>ULD</th>
                                    <th>Shipment Leg</th>
                                    <th>Gross Weight</th>
                                    <th>Volume</th>
                                    <th>Notes</th>
                                    <th class="text-end">Действия</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model.Ulds)
                                {
                                    <tr>
                                        <td>@item.UldDisplay</td>
                                        <td>@(item.ShipmentLegDisplay ?? "-")</td>
                                        <td>@(item.GrossWeightKg?.ToString("0.##") ?? "-")</td>
                                        <td>@(item.VolumeCbm?.ToString("0.###") ?? "-")</td>
                                        <td>@(string.IsNullOrWhiteSpace(item.Notes) ? "-" : item.Notes)</td>
                                        <td class="text-end">
                                            @if (Model.CanManageUlds)
                                            {
                                                <div class="d-flex justify-content-end flex-wrap gap-2">
                                                    <a asp-action="EditShipmentUld"
                                                           asp-route-id="@item.Id"
                                                           class="btn btn-sm btn-outline-light">
                                                        Редакция
                                                    </a>

                                                    <form asp-action="DeleteShipmentUld"
                                                          asp-route-id="@item.Id"
                                                          asp-route-shipmentId="@Model.ShipmentId"
                                                          method="post"
                                                          onsubmit="return confirm('Сигурен ли си, че искаш да изтриеш този shipment ULD?');">
                                                        @Html.AntiForgeryToken()
                                                        <button type="submit" class="btn btn-sm btn-outline-light">
                                                            Изтрий
                                                        </button>
                                                    </form>
                                                </div>
                                            }
                                            else
                                            {
                                                <span class="text-muted">Read only</span>
                                            }
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                }
                else
                {
                    <div class="text-muted">Няма ULDs за този shipment.</div>
                }
            </div>
        </div>
    </div>
</div>