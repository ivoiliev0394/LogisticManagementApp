@model LogisticManagementApp.Models.CompanyPortal.Shipments.References.CompanyShipmentReferencesViewModel

@{
    ViewData["Title"] = "Shipment References";
}

<partial name="_CompanyModuleHeader" />

<div class="row g-4">
    <div class="col-12 col-xl-3">
        <partial name="_CompanyNavigation" />
    </div>

    <div class="col-12 col-xl-9">
        <div class="card">
            <div class="card-body p-4">
                <div class="d-flex justify-content-between align-items-start flex-wrap gap-3 mb-4">
                    <div>
                        <h2 class="h4 mb-1">Shipment References</h2>
                        <p class="text-muted mb-0">@Model.ShipmentNo</p>
                    </div>

                    <div class="d-flex flex-wrap gap-2">
                        @if (Model.CanManageReferences)
                        {
                            <a asp-action="CreateShipmentReference"
                                   asp-route-shipmentId="@Model.ShipmentId"
                                   class="btn btn-primary">
                                Добави reference
                            </a>
                        }

                        <a asp-action="ShipmentDetails"
                               asp-route-id="@Model.ShipmentId"
                               class="btn btn-outline-light">
                            Назад към shipment
                        </a>
                    </div>
                </div>

                @if (Model.References.Any())
                {
                    <div class="table-responsive" data-table-pagination-wrapper>
                        <table class="table align-middle" data-table-pagination="true" data-page-size="8">
                            <thead>
                                <tr>
                                    <th>Reference Type</th>
                                    <th>Reference Value</th>
                                    <th>Description</th>
                                    <th class="text-end">Действия</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model.References)
                                {
                                    <tr>
                                        <td>@item.ReferenceType</td>
                                        <td>@item.ReferenceValue</td>
                                        <td>@(string.IsNullOrWhiteSpace(item.Description) ? "-" : item.Description)</td>
                                        <td class="text-end">
                                            @if (Model.CanManageReferences)
                                            {
                                                <div class="d-flex justify-content-end flex-wrap gap-2">
                                                    <a asp-action="EditShipmentReference"
                                                           asp-route-id="@item.Id"
                                                           class="btn btn-sm btn-outline-light">
                                                        Редакция
                                                    </a>

                                                    <form asp-action="DeleteShipmentReference"
                                                          asp-route-id="@item.Id"
                                                          asp-route-shipmentId="@Model.ShipmentId"
                                                          method="post" data-confirm-message="Сигурен ли си, че искаш да изтриеш този shipment reference?">
                                                        @Html.AntiForgeryToken()
                                                        <button type="submit" class="btn btn-sm btn-outline-light">
                                                            Изтрий
                                                        </button>
                                                    </form>
                                                </div>
                                            }
                                            else
                                            {
                                                <span class="text-muted">Read only</span>
                                            }
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                }
                else
                {
                    <div class="text-muted">Няма references за този shipment.</div>
                }
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}
